class Solution {
    public int romanToInt(String s) {
        int n = s.length();
        int num =0;
        int pre = 0;
        for(int i = n-1 ; i >= 0 ; i-- )
        {
            int value = switch(s.charAt(i))
            {case 'I' ->1;
            case 'I' ->1;
            case 'I' ->1;
            case 'I' ->1;
            

            


            };
        }
    }
}